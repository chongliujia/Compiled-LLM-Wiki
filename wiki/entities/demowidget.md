# DemoWidget

## Summary

DemoWidget is described as a consumer gadget with **public release year 2026**, **standby power under 0.5W** when plugged in but off, and **typical active power up to 12W** in normal use. The statements below are drawn only from the cited claims and the linked source file.

## Source

- Raw bundle: [`raw/sample_demo/`](../../raw/sample_demo/) (see `source.md`)
- `source_id` for IR claims: `sample_demo`

## Claims (IR)

| Claim id | Summary (SPO) | Status |
|----------|----------------|--------|
| `c_demo_001` | DemoWidget — has_public_release_year — 2026 | supported |
| `c_demo_002` | DemoWidget — manual_states_standby_power — under 0.5W when plugged in but powered off | supported |
| `c_demo_003` | DemoWidget — manual_states_typical_active_power — up to 12W during normal use | supported |

## Conflicts

None recorded in `ir/conflicts.json` for this entity.
