# Candidate Claims

This directory stores model-generated candidate claims.

Files here are not ground-truth IR. Review each candidate against the source extract and `raw/` material before patching `ir/claims.json`, `ir/entities.json`, `wiki/`, or `logs/changes.log`.
